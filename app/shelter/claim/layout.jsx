export const metadata = {
  title: "Claim your organization",
  robots: { index: false, follow: false },
  alternates: { canonical: "/shelter/claim" },
};

export default function ClaimLayout({ children }) { return children; }
